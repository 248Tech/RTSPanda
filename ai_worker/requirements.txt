fastapi==0.121.0
uvicorn[standard]==0.38.0
# onnxruntime wheels are available for x86_64 and ARM64 (aarch64/arm64).
# On ARMv7, install will be skipped and the worker can run in degraded mode.
onnxruntime==1.21.0 ; platform_machine == "x86_64" or platform_machine == "AMD64" or platform_machine == "aarch64" or platform_machine == "arm64"
pillow==12.0.0
numpy>=1.24,<3
python-multipart==0.0.20
